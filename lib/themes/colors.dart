import 'package:flutter/material.dart';

class AppColors {
  // Colores principales
  static const Color primaryDark = Color(0xFF1a1a1a);
  static const Color secondaryDark = Color(0xFF222222);
  static const Color accentColor = Colors.blue;

  // Colores para el reproductor
  static const Color playerBackground = Color(0xFF222222);
  static const Color collapsedBackground = Color.fromARGB(255, 29, 29, 33);
  static const Color panelBackground = Color(0xFF222222);

  // Colores de texto
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Colors.white70;
  static const Color textDark = Colors.black;

  // Colores para la lista de música
  static const List<Color> musicTileColors = [
    Colors.deepPurple,
    Colors.indigo,
    Colors.blue,
    Colors.teal,
    Colors.green,
    Colors.amber,
    Colors.orange,
    Colors.deepOrange,
    Colors.red,
    Colors.pink,
    Colors.purple,
    Colors.blueGrey,
  ];
}
